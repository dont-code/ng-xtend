import { DcWorkflowRegistry } from 'dc-workflow';

/*declare global {
  var  WFW_REGISTRY:DcWorkflowRegistry;
}

export function initWorkflowRegistry () {
  if (globalThis.WFW_REGISTRY==null) {
    globalThis.WFW_REGISTRY = new DcWorkflowRegistry();
  }
}

export function dcWorkflowRegistry (): DcWorkflowRegistry {
  if (globalThis.WFW_REGISTRY==null) {
    initWorkflowRegistry();
  }
  return globalThis.WFW_REGISTRY;
}
*/
